<Window x:Class="EEDD.Windows.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EEDD.Windows"
        mc:Ignorable="d" WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Title="Text zápisu" Height="250" Width="400" ResizeMode="NoResize">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox VerticalScrollBarVisibility="Visible" Grid.ColumnSpan="2" Grid.RowSpan="2" Margin="5" AcceptsReturn="True" />

        <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Vertical">
            <Button Content="OK" Width="80" Margin="10,5" Height="24" IsDefault="True" />
            <Button Content="Storno" Width="80" Margin="10,5" Height="24" IsCancel="True" />
        </StackPanel>

        <Label Content="Vzor: " Grid.Row="2" Margin="5,0,0,5" VerticalAlignment="Bottom" />
        <ComboBox Grid.Row="2" Grid.Column="1" Margin="0,0,5,5" VerticalAlignment="Bottom" IsEditable="True">

        </ComboBox>

        <StackPanel Grid.Row="1" Grid.Column="2" Orientation="Vertical" Grid.RowSpan="2" VerticalAlignment="Bottom">
            <Button Content="Smazat vzor" Width="80" Margin="10,5" Height="24" />
            <Button Content="Uložit vzor" Width="80" Margin="10,5" Height="24" />
        </StackPanel>
    </Grid>
</Window>
