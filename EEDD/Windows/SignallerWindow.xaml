<Window x:Class="EEDD.Windows.SignallerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:Common.Controls;assembly=Common.Controls"
        xmlns:local="clr-namespace:EEDD.Windows"
        mc:Ignorable="d" ShowInTaskbar="False"
        Width="300" ResizeMode="NoResize" SizeToContent="Height">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <RadioButton x:Name="TimeSelect" Content="Č_as: " Padding="5" VerticalContentAlignment="Center" IsChecked="True" />
            <c:MaskedTextBox x:Name="Time" Mask="00:00" Grid.Column="3" Margin="5" Height="20" VerticalAlignment="Center" TextChanged="Time_TextChanged" />
            <Label Content="_Den: " Grid.Row="1" HorizontalAlignment="Right" />
            <TextBox x:Name="Date" Grid.Column="3" Grid.Row="1" Margin="5" Height="20" VerticalAlignment="Center" />
            <RadioButton x:Name="Occupied" Content="_Obsazeno" Padding="5" VerticalContentAlignment="Center" Grid.Row="2" IsEnabled="False" Grid.ColumnSpan="2" />
            <RadioButton x:Name="Crossed" Content="_Proškrtnuto" Padding="5" VerticalContentAlignment="Center" Grid.Row="3" IsEnabled="False" Grid.ColumnSpan="2" />
        </Grid>

        <StackPanel Grid.Column="1" Margin="5">
            <Button Content="OK" Width="80" Margin="5" Height="24" IsDefault="True" />
            <Button Content="Storno" Width="80" Margin="5" Height="24" IsCancel="True" />
            <Button Content="Zrušit" Width="80" Margin="5,10" Height="24" />
        </StackPanel>

        <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="5,5,10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label x:Name="TextLabel" Content="Jméno: " />
            <TextBox x:Name="FreeText" Grid.Column="1" VerticalAlignment="Center" Height="20" />
        </Grid>
    </Grid>
</Window>
